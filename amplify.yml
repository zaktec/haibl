version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci        # cleaner install, ensures lockfile integrity
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
